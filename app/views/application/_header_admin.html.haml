.navbar
  .navbar-inner
    =link_to root_path, :class => "brand" do
      Y-Scholars Logger
    %ul.nav
      %li
        =link_to "Dashboard", dashboard_path
    .pull-right
      %ul.nav
        %li
          =link_to "Manage Accounts", users_path
        %li.dropdown
          =link_to "#", :data => {:toggle => "dropdown"} do
            %i.icon-user
            =current_user.name
            %b.caret
          %ul.dropdown-menu
            %li
              =link_to "Logout", logout_path, :method => :destroy
